package pe.cibertec.matricula.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pe.cibertec.matricula.models.FamiliarApoderado;

public interface FamiliarApoderadoRepository extends JpaRepository<FamiliarApoderado, Long> {
}
